# Copy this file to .env.local and fill in your values.
# When VITE_UPLOAD_URL is set, the upload screen performs a real POST request.
# When it is unset, the app falls back to a 1.5 s simulated upload (safe for local dev).
VITE_UPLOAD_URL=https://your-api.example.com/physical-count/upload
